<%= form_for(@availablity) do |f| %>
  <% if @availablity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@availablity.errors.count, "error") %> prohibited this availablity from being saved:</h2>

      <ul>
      <% @availablity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_datetime %><br />
    <%= f.datetime_select :start_datetime %>
  </div>
  <div class="field">
    <%= f.label :end_datetime %><br />
    <%= f.datetime_select :end_datetime %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
