<h1>Listing availablities</h1>

<table>
  <tr>
    <th>Start datetime</th>
    <th>End datetime</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @availablities.each do |availablity| %>
  <tr>
    <td><%= availablity.start_datetime %></td>
    <td><%= availablity.end_datetime %></td>
    <td><%= link_to 'Show', availablity %></td>
    <td><%= link_to 'Edit', edit_availablity_path(availablity) %></td>
    <td><%= link_to 'Destroy', availablity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Availablity', new_availablity_path %>
