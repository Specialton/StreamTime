<%= form_for(@shirt_size) do |f| %>
  <% if @shirt_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shirt_size.errors.count, "error") %> prohibited this shirt_size from being saved:</h2>

      <ul>
      <% @shirt_size.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :size %><br />
    <%= f.text_field :size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
